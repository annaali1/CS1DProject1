#pragma once

/************************************************
 * accountType.hpp holds the enum class
 * AccountType which will determine during login
 * whether or not the user is an administrator
 * or a foodie
 ************************************************/
namespace fastFoodFinder
{
    enum class AccountType {FOODIE, ADMIN};
}
